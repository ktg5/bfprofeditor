name: build & deploy

on:
  push:
    branches: ["main"]

  workflow_dispatch:

permissions:
  contents: write

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      # Bun/Vite stuff
      - name: Setup Bun environment
        uses: oven-sh/setup-bun@v2.2.0
      - name: Install Node packages
        run: bun i
      - name: Run Build Script
        run: bun start

      # Uploaf
      - name: Deploy to GitHub Pages subdirectory
        uses: peaceiris/actions-gh-pages@v4
        with:
          github_token: ${{ secrets.CROSS_REPO_PAT }}
          external_repository: ktg5/ktg5.github.io
          publish_dir: ./dist
          destination_dir: bfprofeditor
          keep_files: true
